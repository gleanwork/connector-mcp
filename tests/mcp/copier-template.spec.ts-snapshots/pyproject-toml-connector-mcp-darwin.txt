[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "test-connector"
version = "0.1.0"
description = "A custom Glean indexing connector"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"

dependencies = [
    "glean-indexing-sdk>=0.3.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
test-connector = "test_connector.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/test_connector"]

[dependency-groups]
dev = [
    "pytest",
    "pytest-cov",
    "ruff",
    "pyright",

]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "W", "N", "UP", "ANN"]
ignore = ["ANN401"]

[tool.pyright]
typeCheckingMode = "basic"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.coverage.report]
fail_under = 80
